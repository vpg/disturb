#!/bin/bash

BASE_DIR=$(readlink -f $(dirname $0))
DISTURB_DIR=$BASE_DIR
if [[ "$BASE_DIR" =~ vendor\/bin$ ]]; then
   DISTURB_DIR="$BASE_DIR/../Vpg/Disturb/bin/"
fi
php $DISTURB_DIR/disturb.php "Vpg\\Disturb\\Tasks\\Manager" ${@}
